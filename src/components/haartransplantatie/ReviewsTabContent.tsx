import { StaticReviewGlass } from './StaticReviewGlass';

export const ReviewsTabContent = () => {
  return (
    <div className="h-full flex items-center justify-center">
      <StaticReviewGlass />
    </div>
  );
};